# Makefile.am -- Process this file with automake to produce Makefile.in

AM_LDFLAGS = -static

bin_PROGRAMS = sav2dsv sav2asm splitfile

sav2dsv_SOURCES	= src/sav2dsv.cpp

sav2asm_SOURCES	= src/sav2asm.cpp

splitfile_SOURCES = src/splitfile.cpp

EXTRA_DIST = autogen.sh
